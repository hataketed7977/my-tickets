# Tasks

<!--
For each implementation slice, replace every placeholder below with a concrete
repository path, test name, command, behavior, and expected result.
-->

## 1. <!-- Behavior or module -->

- [ ] 1.1 RED - Write a failing test in `<exact-test-file>` for `<specific behavior>`
- [ ] 1.2 RED - Run `<exact-focused-test-command>` and confirm it fails because `<expected missing behavior>`
- [ ] 1.3 GREEN - Implement the minimum change in `<exact-production-file-or-files>` needed to pass the test
- [ ] 1.4 GREEN - Re-run `<exact-focused-test-command>` and confirm it passes

<!--
Use the following alternative only for a module where automated testing is
genuinely inapplicable. State the exact file/artifact, a specific reason, and
an exact verification command or observable check. Delete this section
otherwise.
-->

## 2. <!-- Module without an automated test -->

- [ ] 2.1 NO-TEST - Change `<exact-file-or-artifact>`; automated testing is inapplicable because `<specific reason>`; verify with `<exact-command-or-observable-check>`
